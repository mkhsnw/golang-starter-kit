package {{.Snake}}

import (
	"{{.ModuleName}}/internal/foundation/database"
)

type {{.Pascal}} struct {
	database.BaseEntity
{{- range .Fields }}
	{{ .PascalName }} {{ .Type }} `gorm:"column:{{ .SnakeName }};type:{{ .SQLType }}{{ if not .Nullable }};not null{{ end }}"`
{{- end }}
}

func (m *{{.Pascal}}) TableName() string {
	return "{{.Plural}}"
}
